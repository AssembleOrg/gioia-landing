---
import { Mail, Phone, MapPin, MessageCircle, Send, Bot, User } from 'lucide-astro';
---

<section id="contacto" class="py-20 md:py-28 bg-gradient-to-b from-white to-gray-50">
  <div class="container mx-auto px-4 sm:px-6 lg:px-8">
    <!-- Section Header -->
    <div class="text-center mb-16 animate-fade-in-up">
      <h2 class="text-4xl md:text-5xl font-bold text-dark-800 mb-4">
        ¿Necesitas <span class="text-primary-500">Ayuda?</span>
      </h2>
      <p class="text-lg text-gray-600 max-w-2xl mx-auto">
        Estamos aquí para asistirte. Contáctanos por el medio que prefieras
      </p>
      <div class="w-24 h-1 bg-gradient-to-r from-primary-400 to-primary-600 mx-auto mt-6 rounded-full"></div>
    </div>

    <div class="grid lg:grid-cols-2 gap-12 max-w-6xl mx-auto">
      <!-- Contact Form -->
      <div class="bg-white rounded-2xl shadow-2xl p-8 md:p-10">
        <h3 class="text-2xl font-bold text-dark-800 mb-6">Envíanos un Mensaje</h3>
        <form id="contact-form" class="space-y-6">
          <div>
            <label for="name" class="block text-sm font-semibold text-gray-700 mb-2">
              Nombre Completo
            </label>
            <input
              type="text"
              id="name"
              name="name"
              class="w-full px-4 py-3 border-2 border-gray-300 rounded-xl focus:ring-4 focus:ring-primary-200 focus:border-primary-500 transition-all duration-300"
              placeholder="Tu nombre"
              required
            />
          </div>

          <div>
            <label for="email" class="block text-sm font-semibold text-gray-700 mb-2">
              Email
            </label>
            <input
              type="email"
              id="email"
              name="email"
              class="w-full px-4 py-3 border-2 border-gray-300 rounded-xl focus:ring-4 focus:ring-primary-200 focus:border-primary-500 transition-all duration-300"
              placeholder="transportegioiaehijos@gmail.com"
              required
            />
          </div>

          <div>
            <label for="phone" class="block text-sm font-semibold text-gray-700 mb-2">
              Teléfono
            </label>
            <input
              type="tel"
              id="phone"
              name="phone"
              class="w-full px-4 py-3 border-2 border-gray-300 rounded-xl focus:ring-4 focus:ring-primary-200 focus:border-primary-500 transition-all duration-300"
              placeholder="+5491121997046"
            />
          </div>

          <div>
            <label for="message" class="block text-sm font-semibold text-gray-700 mb-2">
              Mensaje
            </label>
            <textarea
              id="message"
              name="message"
              rows="5"
              class="w-full px-4 py-3 border-2 border-gray-300 rounded-xl focus:ring-4 focus:ring-primary-200 focus:border-primary-500 transition-all duration-300 resize-none"
              placeholder="¿En qué podemos ayudarte?"
              required
            ></textarea>
          </div>

          <button
            type="submit"
            class="w-full bg-gradient-to-r from-primary-500 to-primary-600 hover:from-primary-600 hover:to-primary-700 text-white px-8 py-4 rounded-xl font-semibold text-lg transition-all duration-300 transform hover:scale-[1.02] shadow-lg hover:shadow-xl flex items-center justify-center space-x-2"
          >
            <Send size={20} />
            <span>Enviar Mensaje</span>
          </button>
        </form>

        <!-- Success Message (Hidden by default) -->
        <div id="success-message" class="hidden mt-6 bg-green-50 border-2 border-green-500 text-green-800 px-6 py-4 rounded-xl animate-fade-in">
          <p class="font-semibold">¡Mensaje enviado con éxito!</p>
          <p class="text-sm">Nos pondremos en contacto contigo pronto.</p>
        </div>
      </div>

      <!-- Contact Info & WhatsApp -->
      <div class="space-y-6">
        <!-- WhatsApp Cards -->
        <div class="bg-gradient-to-br from-green-500 to-green-600 rounded-2xl shadow-2xl p-8 text-white transform hover:scale-105 transition-all duration-300">
          <div class="flex items-center space-x-3 mb-6">
            <div class="bg-white/20 backdrop-blur-sm p-3 rounded-full">
              <Bot size={32} />
            </div>
            <div>
              <h3 class="text-2xl font-bold">WhatsApp con IA</h3>
              <p class="text-green-100">Atención Automatizada 24/7</p>
            </div>
          </div>
          <p class="mb-6 text-green-50">
            Obtén respuestas instantáneas a tus preguntas más frecuentes con nuestro asistente virtual inteligente.
          </p>
          <a
            href="https://wa.me/5491121997046?text=Hola,%20necesito%20información"
            target="_blank"
            rel="noopener noreferrer"
            class="inline-flex items-center justify-center bg-white text-green-600 hover:bg-green-50 px-6 py-3 rounded-full font-semibold transition-all duration-300 transform hover:scale-105 shadow-lg w-full space-x-2"
          >
            <MessageCircle size={20} />
            <span>Chatear con IA</span>
          </a>
        </div>

        <div class="bg-gradient-to-br from-primary-500 to-primary-600 rounded-2xl shadow-2xl p-8 text-white transform hover:scale-105 transition-all duration-300">
          <div class="flex items-center space-x-3 mb-6">
            <div class="bg-white/20 backdrop-blur-sm p-3 rounded-full">
              <User size={32} />
            </div>
            <div>
              <h3 class="text-2xl font-bold">WhatsApp Personal</h3>
              <p class="text-primary-100">Atención Directa</p>
            </div>
          </div>
          <p class="mb-6 text-primary-50">
            Habla directamente con nuestro equipo para consultas personalizadas y atención especializada.
          </p>
          <a
            href="https://wa.me/5491187654321?text=Hola,%20necesito%20atención%20personalizada"
            target="_blank"
            rel="noopener noreferrer"
            class="inline-flex items-center justify-center bg-white text-primary-600 hover:bg-primary-50 px-6 py-3 rounded-full font-semibold transition-all duration-300 transform hover:scale-105 shadow-lg w-full space-x-2"
          >
            <MessageCircle size={20} />
            <span>Atención Personal</span>
          </a>
        </div>

        <!-- Contact Info Cards -->
        <div class="bg-white rounded-2xl shadow-lg p-8 space-y-6">
          <h3 class="text-xl font-bold text-dark-800 mb-4">Información de Contacto</h3>
          
          <div class="flex items-start space-x-4">
            <div class="bg-primary-100 p-3 rounded-full flex-shrink-0">
              <Phone size={24} class="text-primary-600" />
            </div>
            <div>
              <p class="font-semibold text-dark-800">Teléfono</p>
              <p class="text-gray-600">+5491121997046</p>
            </div>
          </div>

          <div class="flex items-start space-x-4">
            <div class="bg-primary-100 p-3 rounded-full flex-shrink-0">
              <Mail size={24} class="text-primary-600" />
            </div>
            <div>
              <p class="font-semibold text-dark-800">Email</p>
              <p class="text-gray-600">transportegioiaehijos@gmail.com</p>
            </div>
          </div>

          <div class="flex items-start space-x-4">
            <div class="bg-primary-100 p-3 rounded-full flex-shrink-0">
              <MapPin size={24} class="text-primary-600" />
            </div>
            <div>
              <p class="font-semibold text-dark-800">Dirección</p>
              <p class="text-gray-600">Camilo Cavour 3101, B1824 Lanús, Provincia de Buenos Aires</p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<script>
  const contactForm = document.getElementById('contact-form');
  const successMessage = document.getElementById('success-message');

  if (contactForm) {
    contactForm.addEventListener('submit', (e) => {
      e.preventDefault();
      
      // Simulate form submission
      if (successMessage) {
        successMessage.classList.remove('hidden');
        
        // Scroll to success message
        setTimeout(() => {
          successMessage.scrollIntoView({ behavior: 'smooth', block: 'nearest' });
        }, 100);
        
        // Reset form
        (contactForm as HTMLFormElement).reset();
        
        // Hide success message after 5 seconds
        setTimeout(() => {
          successMessage.classList.add('hidden');
        }, 5000);
      }
    });
  }
</script>

